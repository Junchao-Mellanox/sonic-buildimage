# Base docker build
FROM docker-orchagent-sonic:latest

# Copy the cache data to host
From scratch as output
COPY --from=docker-orchagent-sonic:latest /cache.tgz cache.tgz

# Clean up the cache data
FROM docker-orchagent-sonic:latest as final
RUN rm /cache.tgz

